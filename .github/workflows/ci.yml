steps:
  # 1. 下載程式碼
  - uses: actions/checkout@v3
  
  # 2. 設定 Node.js
  - uses: actions/setup-node@v3
    with:
      node-version: '18'
  
  # 3. 安裝依賴
  - run: npm ci
  
  # 4. 執行測試
  - run: npm test
  
  # 5. 執行 Lint
  - run: npm run lint 
